# Paths preserved across deploys (deploy-branding-guru.sh).
# These live beside the build, not inside dist/, so without this file the
# deploy wipes them. It did exactly that on Sep 7 2026 to the whole library.
library
